# 8

# Dimenzije objekta

# Funkcija dim() se koristi za ispitivanje ili definisanje dimenzija objekta (vektora, matrice, nizova ili tabela). 
# Vektor je moguce preoblikovati ovom funkcijom u cilju dobijanja n-dimenzionog niza.
# Koristeci vektor iz prikazanog primera na pocetku poglavlja kreira se matrica.
v <- c(2,6,7,7)
dim(v) <- c(2,2)
v

is.array(v)
is.vector(v)

# Moguce je proveriti i koliko dimenzija ima objekat.
dim(v)
